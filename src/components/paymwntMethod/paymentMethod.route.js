// import express from "express";
// import { login, register } from "./paymentMethod.controller.js";
// import { loginSchema, registerSchema } from "./paymentMethod.validate.js";
// import { validateSchema } from "../../middleware/validation.js";
// const userRoute = express.Router();

// userRoute.post("/register", validateSchema(registerSchema), register);
// userRoute.post("/login", validateSchema(loginSchema), login);

// export default userRoute;
